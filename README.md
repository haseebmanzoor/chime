### Install Npm dependencies (android+iOS Only)

- `npm i`
- `cd ios && pod install` for case of ios
- `cd .. && react-native run-ios/android`

# Or Alternatively you can use android studio to build gradle by opening android folder.
